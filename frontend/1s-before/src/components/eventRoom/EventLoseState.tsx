import { EventWinOrLoseStateView } from '@/types/eventRoom';

export default function EventLoseState({
  eventId,
  joinedAt,
  ranking,
}: Readonly<EventWinOrLoseStateView>) {
  return <div>dd</div>;
}
